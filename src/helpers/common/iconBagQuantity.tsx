import Svg from "react-native-svg";

export const iconBagQuantity = ( quantity: string ) => {

    switch (quantity) {
        case '1P':
            return (
                <Svg 
                />
            )
            break;
    
        default:
            break;
    }

}